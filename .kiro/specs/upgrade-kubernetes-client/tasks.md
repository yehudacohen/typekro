# Implementation Plan

- [x] 1. Create centralized error handling utilities
  - [x] 1.1 Create `src/core/kubernetes/errors.ts` with error utilities
    - Implement `getErrorStatusCode()` function to extract status codes from various error shapes
    - Implement `isNotFoundError()`, `isConflictError()`, `isRetryableError()` functions
    - Implement `formatKubernetesError()` for consistent error messages
    - Add JSDoc comments for all public functions
    - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - [x] 1.2 Write property test for error handling utilities
    - **Property 1: Error Handling Consistency**
    - **Validates: Requirements 2.1, 2.4**
  - [x] 1.3 Export error utilities from kubernetes module index
    - Update `src/core/kubernetes/index.ts` to export new utilities
    - _Requirements: 2.1_

- [x] 2. Create type guards module
  - [x] 2.1 Create `src/core/kubernetes/type-guards.ts`
    - Implement `isKubernetesResponse()` type guard
    - Implement `isKubernetesError()` type guard
    - Implement `hasStatusCode()` type guard
    - Add JSDoc comments for all functions
    - _Requirements: 4.1, 4.4_
  - [x] 2.2 Export type guards from kubernetes module index
    - _Requirements: 4.1_

- [x] 3. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 4. Upgrade @kubernetes/client-node package
  - [x] 4.1 Update package.json to main branch (includes fix for #2670)
    - Run `bun remove @kubernetes/client-node`
    - Run `bun add @kubernetes/client-node@github:kubernetes-client/javascript#main`
    - Alternative: Pin to specific commit with PR #2688: `bun add @kubernetes/client-node@github:kubernetes-client/javascript#e91507a`
    - Verify bun.lock is updated
    - Note: Using main branch because 1.4.0 has loadYaml regression (#2670), fixed in PR #2688
    - When 1.5.0 is released, switch to `bun add @kubernetes/client-node@1.5.0`
    - _Requirements: 1.1, 1.2_
  - [x] 4.1.1 Verify git package installation
    - Check that the package installed correctly from git
    - Verify types are available (may need to build the package)
    - If build issues occur, consider using a fork or waiting for 1.5.0
    - _Requirements: 1.1_
  - [x] 4.2 Run typecheck to identify breaking changes
    - Run `bun run typecheck`
    - Document any type errors that need to be fixed
    - _Requirements: 5.1, 5.2_
  - [x] 4.3 Fix breaking API changes in source code
    - Updated all `.body` references - new API returns objects directly
    - Updated API method signatures - methods now take request objects
    - Updated `getCachedClient` signature for new constructor types
    - Fixed `exactOptionalPropertyTypes` issue in cluster config
    - Updated `V1Subject` to `RbacV1Subject`
    - Updated `from` to `_from` in NetworkPolicy ingress rules
    - Library (`typecheck:lib`) and examples (`typecheck:examples`) now pass
    - _Requirements: 5.1, 5.2, 5.3_
  - [-] 4.4 Fix test type errors (198 remaining, down from 239)
    - **Progress**: Library (`typecheck:lib`) and examples (`typecheck:examples`) pass ✅
    - **Fixed files**:
      - `test/integration/shared-kubeconfig.ts` - Updated API client creation
      - `test/integration/alchemy-deployment-strategy-integration.test.ts` - Fixed deleteNamespace
      - `test/integration/cert-manager/certificate.test.ts` - Fixed CustomObjectsApi calls
      - `test/integration/e2e-cluster.test.ts` - Fixed most API calls (26→2 errors)
    - **Remaining issues** (all in test files):
      - API method signature changes (methods now take request objects instead of positional args)
      - Stricter V1ObjectMeta type (tests create mock resources with minimal metadata)
      - Schema proxy type issues in tests
      - Some Cel.expr calls need type parameters
    - **Pattern for fixes**:
      - Old: `api.method(arg1, arg2, arg3)`
      - New: `api.method({ prop1: arg1, prop2: arg2, prop3: arg3 })`
      - Old: `response.body.field`
      - New: `response.field` (no .body wrapper)
    - _Requirements: 5.1, 5.2_

- [ ] 5. Update error handling across codebase
  - [ ] 5.1 Update `src/core/kubernetes/client-provider.ts`
    - Import and use centralized error utilities
    - Update `defaultRetryableErrorCheck` to use new utilities
    - Ensure retry logic works with fetch-based errors
    - _Requirements: 2.1, 7.1, 7.2, 7.3_
  - [ ] 5.2 Write property test for retry logic
    - **Property 3: Retry Logic Correctness**
    - **Validates: Requirements 7.1, 7.2, 7.3**
  - [ ] 5.3 Update `src/core/kubernetes/api.ts`
    - Use centralized error utilities for error handling
    - Update catch blocks to use `isNotFoundError()`, `isConflictError()`
    - _Requirements: 2.2, 2.3_
  - [ ] 5.4 Update `src/factories/kubernetes/yaml/yaml-file.ts`
    - Replace inline error status code checks with utility functions
    - _Requirements: 2.2, 2.3_
  - [ ] 5.5 Update `src/factories/kubernetes/yaml/yaml-directory.ts`
    - Replace inline error status code checks with utility functions
    - _Requirements: 2.2, 2.3_

- [ ] 6. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 7. Update Watch API usage
  - [ ] 7.1 Verify Watch API compatibility in `src/core/deployment/event-monitor.ts`
    - Ensure `new k8s.Watch(kubeConfig)` works correctly
    - Verify watch callback signatures are compatible
    - Test error callback handling
    - _Requirements: 3.1, 3.2, 3.3_
  - [ ] 7.2 Create `src/core/kubernetes/watch.ts` with ManagedWatch abstraction
    - Implement `ManagedWatch` class with automatic reconnection
    - Add error recovery and resource version tracking
    - Add JSDoc comments
    - _Requirements: 3.1, 3.2, 3.3_
  - [ ] 7.3 Export watch utilities from kubernetes module index
    - _Requirements: 3.3_

- [ ] 8. Update KubeConfig handling
  - [ ] 8.1 Verify KubeConfig API compatibility
    - Test `loadFromDefault()`, `loadFromFile()`, `loadFromCluster()`
    - Verify `makeApiClient()` works correctly
    - Test cluster, user, and context configuration
    - _Requirements: 4.2, 4.3, 6.1_
  - [ ] 8.2 Write property test for KubeConfig configuration
    - **Property 2: KubeConfig Configuration Correctness**
    - **Validates: Requirements 4.2, 6.2, 6.3, 6.4**
  - [ ] 8.3 Verify authentication mechanisms
    - Test token-based authentication
    - Test certificate-based authentication
    - Test skipTLSVerify configuration
    - _Requirements: 6.2, 6.3, 6.4_

- [ ] 9. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 10. Create kubernetes utilities module
  - [ ] 10.1 Create `src/core/kubernetes/utils.ts`
    - Implement `normalizeResourceResponse()` function
    - Implement `extractResourceVersion()` function
    - Implement `buildResourcePath()` function
    - Add JSDoc comments
    - _Requirements: 4.4_
  - [ ] 10.2 Export utilities from kubernetes module index
    - _Requirements: 4.4_

- [ ] 11. Update factory files to use centralized types
  - [ ] 11.1 Audit factory files for direct @kubernetes/client-node imports
    - Identify files importing types directly from @kubernetes/client-node
    - Document which types need to be added to centralized location
    - _Requirements: 4.1_
  - [ ] 11.2 Add any missing types to `src/core/types/kubernetes.ts`
    - Add any V1* types not already re-exported
    - _Requirements: 4.1_
  - [ ] 11.3 Update factory files to import from centralized location
    - Update imports in `src/factories/kubernetes/` files
    - Ensure all type imports come from `../../core/types/kubernetes.js`
    - _Requirements: 4.1_

- [ ] 12. Run full test suite
  - [ ] 12.1 Run unit tests
    - Run `bun run test`
    - Fix any failing tests
    - _Requirements: 1.3_
  - [ ] 12.2 Run typecheck
    - Run `bun run typecheck`
    - Fix any type errors
    - _Requirements: 5.2_
  - [ ] 12.3 Run integration tests (if cluster available)
    - Run `bun run test:integration`
    - Verify Watch functionality works
    - Verify CRUD operations work
    - _Requirements: 1.4, 3.1, 4.4_

- [ ] 13. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [ ] 14. Documentation and cleanup
  - [ ] 14.1 Update kubernetes module index exports
    - Ensure all new modules are properly exported
    - _Requirements: 1.1_
  - [ ] 14.2 Remove any deprecated code
    - Remove any workarounds for old client version
    - Clean up any temporary compatibility code
    - _Requirements: 1.2_
  - [ ] 14.3 Verify no remaining direct @kubernetes/client-node imports in factories
    - Search for remaining direct imports
    - Update any missed files
    - _Requirements: 4.1_
